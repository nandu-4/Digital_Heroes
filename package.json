{
  "name": "page-pulse",
  "version": "1.0.0",
  "description": "Audit any URL: status, speed, title, meta, headings, images, word count.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.0"
  }
}
